{% extends 'base.html.twig' %}

{% block body %}
<h1>XSS</h1>
<p>L'objectif est d'utiliser une injection XSS afin d'exécuter du code javascript dans la page, et de "défigurer" la page.</p>
<p>Ce code doit, par exemple, ajouter une image en haut de page (utilisez JQuery)</p>

<h2>Ajouter un article</h2>
{{ form_start(form) }}
{{ form_widget(form) }}
<button type="submit" class="btn btn-default">Submit</button>
{{ form_end(form) }}
<p></p>
<h2>
Les articles
</h2>
{% for article in articles %}
<div class="card mb-2">
  <h5 class="card-header">
    {{ article.title }}
  </h5>
  <div class="card-body">
    <h6 class="card-subtitle mb-2 text-muted">
      Published :
      {{ article.date | date }}
    </h6>
    <p class="card-text">
      {{ article.content | raw }}
    </p>
  </div>
</div>
{% endfor %}

{% endblock %}